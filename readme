上面是我的代码.
他的主要功能是呈现一个或多个puml文件, 每个窗口呈现一个puml文件, 并在文件有修改时重新加载窗口.
我现在想再加一个功能, 就是当我按下command c组合键时, 把当前puml窗口做一个截图, 然后保存到系统粘贴板.
这个代码只在macos上运行, 而且command c组合键, 只在当前puml窗口上起作用. 
也就是说, 这个程序不激活时, 或者说当前激活的是其他程序时, command c组合键, 还是他之前的行为. 不要修改.
生成需要修改的文件
我想用一个单独的分支来实现这个功能, 帮我给这个分支起一个好的名字.
回答用中文.
