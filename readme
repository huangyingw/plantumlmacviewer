上面是我的代码.
他的主要功能是呈现一个或多个puml文件, 每个窗口呈现一个puml文件, 并在文件有修改时重新加载窗口.
我现在想再加一个功能, 当我按下command =时, 就把当前窗口放大, 按下command -时, 就把当前窗口缩小.
上下左右四个方向键按下时, 就相应的移动图片.
生成需要修改的文件
我想用一个单独的分支来实现这个功能, 顺便帮我给这个分支起一个好的名字.
回答用中文.
